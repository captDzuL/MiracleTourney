import type { Certificate, CertificateStatus } from "@/lib/platform/types";

export type { Certificate, CertificateStatus };

export type CertificateAssetUpdates = {
  characterArtUrl?: string;
  accentColor?: string;
};
