import { workspacePage, type WorkspacePageProps } from "@/lib/competition/workspace-page";
export default async function MatchPage(props: WorkspacePageProps) { return workspacePage(props, "match"); }
