import { workspacePage, type WorkspacePageProps } from "@/lib/competition/workspace-page";
export default async function MatchControlPage(props: WorkspacePageProps) { return workspacePage(props, "match-control"); }
