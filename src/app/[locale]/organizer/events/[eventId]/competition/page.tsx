import { workspacePage, type WorkspacePageProps } from "@/lib/competition/workspace-page";
export default async function CompetitionPage(props: WorkspacePageProps) { return workspacePage(props, "competition"); }
