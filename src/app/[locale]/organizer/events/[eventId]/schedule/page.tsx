import { workspacePage, type WorkspacePageProps } from "@/lib/competition/workspace-page";
export default async function SchedulePage(props: WorkspacePageProps) { return workspacePage(props, "schedule"); }
