# Miracle V3 Release 1.0 Verification — Organizer Master Workspace

Date: 2026-09-24 Asia/Jakarta (final-review fix wave)
Branch: `codex/organizer-release-readiness`
Source/product base before this final-review fix wave: `b97b9e80aa92806e23cfb93752f2cf4c630cbe09`
Target: `feature/ui/release/1.0`
PR: not created (release evidence is incomplete)

## Decision

**BLOCKED.** The branch is not authorized for deployment, promotion, production
migration, production restore, or production feature-flag activation. This is a
factual evidence report, not a READY claim.

The decision remains mechanically BLOCKED by the local pressure RED recorded
below and the outstanding final-SHA GitHub CI, credentialed
preview/log/RUM/Vercel rollback/PIC, Neon recovery-console/restore-rehearsal/
migration-integration, and live 64-team query/p95 evidence listed below. The fresh scoped 5.6-Sol/high re-review of
this fix wave is still pending and is not represented as approval. No
deployment, production-setting mutation, database migration, restore, flag
activation, force-push, push, or PR was performed.

## Final-review fix wave — 2026-09-24

The whole-branch review identified two implementation P2s and one historical
evidence-freshness note. The bounded remediation is included in the final fix wave:

- **P2 process-local limiter:** rate-limited actions now use the shared
  `RateLimitBucket` Prisma table with hashed logical keys, compare-and-swap
  increments, bounded cleanup, and fail-closed database/contention handling.
  The process-local tracker is bounded and deny-only; middleware keeps its
  conservative edge check while action call sites await the authoritative
  shared decision. Concurrency, digest-redaction, expiry, failure, and call
  site tests cover the contract.
- **P2 mixed-version session/reset rollout:** `sessionVersion=0` accepts
  pre-version tokens only while the user row remains at zero; new reset rows
  store SHA-256 digests, while explicitly identified legacy raw rows retain a
  bounded compatibility window and the exact 30-minute validity contract.
  Migration comments document digest invalidation on rollback, with tests for
  expiry, reuse, race, and one-time session revocation.
- **Stale official evidence:** this report, the handoff records, and the
  ignored release-readiness reports now distinguish the `b97b9e8` pre-fix
  local matrix from the final-fix verification, preserve the terminal-gate
  results, and leave the fresh scoped review explicitly pending.

The authoritative pre-fix local matrix at `b97b9e8` was Prisma valid,
TypeScript exit 0, ESLint 0 errors/56 warnings, unit 207 passed/2 skipped
files and 2,433 passed/6 intentional skips, DB-free smoke 24/24, pressure
`/id/login` `/api/me` `/id/admin` p95 2,370/414/52 ms with zero failures,
audit clean, and production build 46/46. Terminal gates are CLOSED rather
than blockers: legacy 4/4; split visual V3 24/24 plus V2 9/9 with zero
skips/retries/flakes; and warmed quick-load routes all HTTP 200 with p97.5
2,945/2,069/1,582 ms and zero errors.

The fresh bounded final-fix checks are recorded in
`.superpowers/sdd/2026-09-20-organizer-master-workspace-release-readiness/whole-branch-final-fix-report.md`.
They include no-DB Prisma generate/validate, nonincremental TypeScript,
actual ESLint, one complete Vitest run plus the affected-file repair check,
DB-free smoke, DB-free pressure, moderate audit, production build, diff
hygiene, and final status. The current pressure environment produced historical
`/id/login` p95 samples of 4,387 ms and 3,783 ms with zero request failures;
those samples are superseded by the exact final RED record below, and the
3,000 ms threshold was not changed.

## Scope and provenance

All work in this report was performed in the isolated worktree
`C:\Users\dzulf\.codex\worktrees\organizer-release-readiness\MiracleTourney-gitnative`.
The dirty checkout at `E:\dev\MiracleTourney-gitnative` was not inspected or
modified. The ignored SDD ledger remains orchestrator-owned and was not edited
by Task 12.

The evidence sources read for this pass were the release-readiness plan and
Task 12 brief, the complete Task 1–11 reports, review/re-review packages, the
existing handoff/PRD/snapshot/progress documents, repository status/history,
and the existing operations/testing runbooks. No secret-bearing environment
file was read or copied. The ignored SDD ledger received only the bounded
final-review fix-wave addendum; no historical task record was rewritten.

## Historical Task 12 identity (superseded by the final-review fix wave above)

| Item | Value | Evidence |
| --- | --- | --- |
| Worktree | `C:\Users\dzulf\.codex\worktrees\organizer-release-readiness\MiracleTourney-gitnative` | isolated checkout |
| Branch | `codex/organizer-release-readiness` | `git branch --show-current` |
| Source/product SHA | `79e62cddc77beae4913529d471b493c6e56335b3` | `git rev-parse HEAD` before docs |
| Evidence-doc commit | `7cdb39df8810045ebdddf8384a55d9f147608fb9` | docs-only commit created after the source/product SHA; reported separately |
| Release base | `e2f7c289f25954780e4de8eff71a9cbc5121e00c` | `origin/feature/ui/release/1.0` |
| Merge-base | `e2f7c289f25954780e4de8eff71a9cbc5121e00c` | baseline report |
| Ancestry | `0 behind / 7 ahead` at Task 1 baseline | baseline report |
| Target | `feature/ui/release/1.0` | release plan |
| PR | none | intentionally not created while BLOCKED |

The evidence-doc commit is intentionally reported separately in the Task 12
report and handoff after Git creates it; the source/product SHA above is the
exact implementation HEAD reviewed by the local evidence matrix.

## Task commits and review disposition

The following map is reconstructed from the release-readiness ledger, reports,
review packages, and `git log`. “Implementation review clean” describes the
static/specification/code-quality review recorded by the task artifacts; it
does not certify missing database-backed or preview evidence.

| Task | Commits in the release-readiness range | Review/evidence disposition |
| --- | --- | --- |
| 1 | `b5af4f2`, `ac8a858` | Plan/ledger review fix rounds complete; review clean after correction; release remains BLOCKED. |
| 2 | `fcaa5a8`, `dc0e54c`, `acd2420` | Static contract/review clean; guarded Playwright, viewport, branded-error artifact and zero-skip evidence BLOCKED by missing `.env.test`. |
| 3 | `24d458c` | Implementation review clean; serial Neon four-format/P2028 evidence BLOCKED by missing authorized `.env.test`. |
| 4 | `f6e07b3`, `0525d7c` | Implementation review clean; serial Neon publication/history/geometry evidence BLOCKED by missing authorized `.env.test`. |
| 5 | `3a4ac2b`, `a7a3d23`, `6c5fa6e` | Runner/config/action-major review clean; credentialed serial CI/browser runtime BLOCKED by missing `.env.test`. |
| 6 | `23a9da6`, `92bd63f`, `99eb4c7`, `3e3b4c5` | Implementation review clean after three fix rounds; live DB/browser manipulated-ID and no-partial-write evidence BLOCKED. |
| 7 | `f775351`, `fc506f1`, `4bb12d8` | Implementation review clean after two fix rounds; Delicate/preview migration diff, reset E2E, and guarded DB evidence BLOCKED. |
| 8 | `7966682`, `976141e`, `66b2412`, `11f1fcc` | Implementation review clean after three fix rounds; credentialed browser/live DB/preview/load evidence BLOCKED. |
| 9 | `d3c3ba6`, `b17cc9d`, `d3bc602`, `352b476`, `37c10a1`, `c486805`, `8f76d5a`, `3f1cc40` | Implementation review clean after four fix rounds; live Neon/query trace, pressure, browser/preview and load-target evidence BLOCKED. |
| 10 | `63a701a`, `4ef5143`, `ee32458`, `7868462` | Implementation review clean after three fix rounds; Vercel access, preview/log scan, saved views and deployed RUM evidence BLOCKED. |
| 11 | `dd28d83`, `b181f68`, `dec5112`, `59cf159`, `2f010a1`, `79e62cd` | Static implementation/review clean after five rounds; credentialed shared-Neon browser and all 20 visual baselines BLOCKED. |
| 12 | evidence docs in this commit | Local safe gates recorded below; final review/CI/preview/recovery gates remain BLOCKED. |

Known deferred/minor evidence is preserved rather than promoted to a pass:
Task 3 report wording around the initial Playwright binary failure, the
synthetic-vs-live authorization/performance distinctions, and all external
runtime limitations remain explicit in the task reports and this decision.

## Historical Task 12 safe local command evidence

The original Task 12 command matrix was serial and credential-independent. Exact command,
exit status, duration, and test counts are recorded in
`.superpowers/sdd/2026-09-20-organizer-master-workspace-release-readiness/task-12-report.md`.
Commands that require a guarded test database, network service, or deployment
are not repeated here; their prior exact failures are recorded below.

| Gate | Required evidence | Status at this SHA |
| --- | --- | --- |
| TypeScript | `node node_modules/typescript/bin/tsc --noEmit --incremental false` | **PASS**, no diagnostics; approximately 15.0 s |
| Second TypeScript invocation | `pnpm lint` plus elevated rerun | **PASS**, but this script invokes `tsc --noEmit`; it is recorded as a second typecheck, not ESLint. Rerun 6.09 s; first 6.35 s attempt was sandbox EPERM writing `tsconfig.tsbuildinfo` |
| ESLint | `node node_modules/eslint/bin/eslint.js . --quiet` | **PASS**, exit 0 in 9.325 s; actual repository ESLint binary |
| Unit | `node node_modules/vitest/vitest.mjs run --reporter=dot` | **BLOCKED/RED**, 200 files; 196 passed, 2 failed, 2 skipped; 2,311 passed, 2 failed, 6 skipped; 43.03 s |
| Install | `pnpm install --frozen-lockfile` | **NOT RUN / BLOCKED**, not repeated because dependencies were already present; the release plan still lacks a fresh install artifact |
| Smoke | `pnpm test:e2e:smoke` | **NOT RUN / BLOCKED**, guarded `.env.test`, Delicate/preview target, and preview URL are absent |
| Pressure | `pnpm test:pressure:smoke` | **NOT RUN / BLOCKED**, guarded Neon input, pressure target, and dashboard/performance inputs are absent |
| E2E | `pnpm test:e2e:ci` | **NOT RUN / BLOCKED**, guarded `.env.test`, preflight/prepare inputs, and credentialed preview target are absent |
| Build | elevated `pnpm build` | **PASS**, 46/46 pages; 53.81 s; expected missing local `DATABASE_URL` prerender warning only |
| Prisma schema | `prisma validate` | **BLOCKED**, Prisma engine download hit `ECONNREFUSED`; no `DIRECT_URL`/guarded DB supplied |
| Diff hygiene | `git diff --check` | **PASS**, 55 ms; no whitespace errors |
| Audit | `pnpm audit --offline --audit-level moderate` | **BLOCKED**, pnpm attempted registry advisory request; `ECONNREFUSED`; no result claimed |

The prior task reports already provide credential-independent focused/static
passes, including Task 10 production build `46/46`, Task 7/8 full unit passes,
and Task 11 type/lint/secret/diff checks. Those are not substituted for this
final command matrix; the fresh results are captured in the Task 12 report.

The fresh full unit run is a release blocker at this SHA. The two failures are
`src/lib/events/public-ongoing-route.test.ts` (expected 503, received 500) and
`src/lib/registration/registration-v3-contract.test.ts` (missing mocked reader
context caused `TypeError: Cannot read properties of undefined (reading
'length')` in `assertReaderResultWithinLimit`). This docs-only task does not
alter product code or rerun the suite.

Repository/code-owner action is required for both failures: inspect the public
ongoing-route error mapping and the registration contract fixture/reader
boundary, make the smallest appropriate product-or-test fix in a new commit,
then rerun the complete `node node_modules/vitest/vitest.mjs run --reporter=dot`
suite on that resulting HEAD to a normal exit 0. A focused test pass must not
substitute for this full-suite rerun, and this Task 12 docs pass does not claim
that either failure is resolved. This owner/action is separate from the
external owners below who must provide CI, preview, database, and recovery
evidence.

## CI, preview, and external evidence

| Gate | Status | Exact missing evidence and required owner/action |
| --- | --- | --- |
| GitHub Actions for final SHA | **BLOCKED** | No final-SHA workflow URL/run result was supplied. A repository maintainer must push normally and provide the completed CI run; no force-push was made. |
| Local full-suite failures | **BLOCKED** | Repository/code owner must resolve the two recorded unit failures and rerun the complete suite on the resulting HEAD; focused tests do not close this gate. |
| Vercel preview URL/deployment ID | **BLOCKED** | No authorized Vercel project/session, preview URL, or deployment ID is available. A Vercel PIC must provide a non-production preview and known-good deployment ID. |
| Preview route matrix | **BLOCKED** | ID/EN, organizer/admin roles, non-owner denial, registration/import/payment/QRIS, all four formats, score/statistics, Completion, seven certificates, mobile/desktop, and flag-off rollback were not run against a credentialed preview. |
| Preview log scan | **BLOCKED** | No Runtime Logs export or preview error scan is available. |
| Saved views/PIC/RUM | **BLOCKED** | Operations runbook documents five saved-view definitions, but no creation/access proof, PIC identity, deployment-failure notification proof, or deployed Speed Insights/RUM sample was supplied. |
| Neon snapshot/PITR | **BLOCKED** | Snapshot/PITR availability, retention, RPO, RTO, restore owner, and switchover decision were not externally verified. |
| Neon restore rehearsal | **BLOCKED** | No fresh Delicate/preview restore rehearsal or post-restore integrity-query output exists. Production restore remains human-authorized and out of scope. |
| Migration review | **BLOCKED** | Task 7 static migration review passed, but the required Delicate/preview migration diff/status needs an authorized guarded DB and shadow DB; no migration was applied. |
| Shared-Neon E2E/performance | **BLOCKED** | `.env.test` with guarded `DATABASE_URL`/`DIRECT_URL`, fixture namespace, pressure target, and dashboard performance inputs were not supplied. |
| Whole-branch final review | **BLOCKED** | No fresh Sol/high whole-branch verdict from the release merge-base to this SHA is present; the parent orchestrator must obtain it once the evidence package is reviewable. |
| PR | **NOT CREATED** | PR creation is withheld until every required gate is evidenced and final review has no P0/P1/P2. |

## Security pre-deployment checklist

An unchecked or unverifiable item keeps the release BLOCKED.

| Checklist item | Status | Evidence/blocker |
| --- | --- | --- |
| Auth/isolation matrix, manipulated IDs, no partial writes, audited platform-admin exception | **PARTIAL / BLOCKED** | Synthetic/direct tests are green; live Neon/browser matrix and all child-ID/no-partial-write paths are not evidenced. |
| Password reset: 30-minute TTL, digest, expiry/reuse/race, session revocation, enumeration, rate limits | **PARTIAL / BLOCKED** | Unit/static contracts are green; guarded reset E2E and Delicate/preview migration diff are unavailable. |
| Injection/XSS/schema/raw SQL/formula/MIME/path traversal | **PARTIAL / BLOCKED** | Credential-independent negative/security suites and scans are green; complete runtime/API/preview coverage is not evidenced. |
| API exposure: role/ownership, same-origin, CORS, cache, rate limits | **PARTIAL / BLOCKED** | Touched-route/action contracts are green; live/preview behavior and exhaustive inventory coverage are not evidenced. |
| Safe errors/log redaction/no credentials or PII in artifacts | **PARTIAL / BLOCKED** | Static logger/security tests and scans are green; preview log/artifact scan is unavailable. |
| Performance: 64-team fixture, N+1, bounded lists, p95, browser budgets, Speed Insights | **BLOCKED** | Mocked/instrumented bounds are green; no live fixture/query trace, pressure/load target, preview browser metrics, or deployed RUM. |
| Monitoring: searchable request-ID logs, notification, saved views, PIC, clean preview scan | **BLOCKED** | Code/runbook evidence exists; Vercel project access, saved-view/PIC/notification proof, and preview scan are missing. |
| Recovery: Vercel known-good rollback, Neon snapshot/PITR/retention/RPO/RTO/PIC, restore rehearsal, flag rollback | **BLOCKED** | Feature-flag rollback is statically documented; external deployment/database IDs, rehearsal, and owner evidence are missing. |
| Release evidence: all commands, final-SHA CI, preview, final review, no required skips/flaky retries | **BLOCKED** | Local matrix is limited to safe commands; guarded runtime, CI, preview, and final review are not complete. |

## Recovery/runbook state

The application rollback mechanism is the existing
`FEATURE_FLAG_ORGANIZER_MASTER_SHELL_V3=false` (or omission), which restores
the prior composition without reversing schema/operation commits. This is a
code-level/runbook fact, not a tested production action. The exact command
shape requested by the plan is `vercel rollback <known-good-deployment-id>`;
the deployment ID, PIC, validated artifact, and non-production rehearsal are
missing, so the command is intentionally not executed.

Neon recovery remains unverified. Required external entries are: snapshot/PITR
availability and retention; measured RPO/RTO; PIC; switchover decision; exact
post-restore integrity queries; and a fresh Delicate/preview restore rehearsal.
No production restore or migration was attempted.

## Final disposition

`BLOCKED` is the only supported decision. The local pressure RED
(`/id/login` p95 `4,197 ms`, max `4,199 ms`, failures `0`, exit `1`) and the
named human/external owners' missing guarded database, CI, preview/Vercel,
monitoring, RUM, recovery, and whole-branch-review evidence must be resolved
before the branch may proceed to a normal push/PR/release review and the full
checklist is green.

## Final bounded migration-safety correction — 2026-09-24

The security implementation is `44a3068a7e7296c1da50a6fcd5b4e08241bb1860`;
the migration guard and docs/static correction are `76f62641693c8287896140f6d566fdd2a794d92f`, followed by this docs-only update. The password-reset migration performs a PostgreSQL `DO` preflight that groups `PasswordResetToken` rows by `userId`, raises an actionable exception when duplicates exist, and only then creates `PasswordResetToken_userId_key`. It contains no delete, deduplication, update, insert, truncate, or merge of legacy rows.

Focused verification remained DB-free: migration static contract 1 file/2 tests passed, TypeScript exit 0, Prisma validate with dummy URLs was valid, changed-file ESLint exit 0, and diff hygiene was clean. The final supplied Vitest evidence is 209 passed / 2 skipped files and 2,445 passed / 6 skipped tests, exit 0; it was not rerun for this docs-only correction. The final pressure record is exact and was not retried: 80 requests at concurrency 20 against `/id/login`, p95 `4,197 ms`, max `4,199 ms`, failures `0`, exit `1`; later scenarios stopped. No pressure, full-suite, E2E, reset, seed, migration application, deployment, push, or PR was run here.

The six suite skips are intentional: one guarded migration-DB integration and five opt-in installed-browser renderer tests. The local release status remains **BLOCKED** by the pressure RED above plus final-SHA CI; authorized Vercel preview/logs/RUM/rollback/PIC evidence; Neon recovery/restore and migration-integration evidence; live 64-team query/p95/load evidence; and fresh scoped Sol/high review. Both local and external gates remain represented.

### Human-gated duplicate `userId` migration procedure

Pause password-reset issuance and obtain a named release/security/data owner. Run the following as separate human-gated commands on the authorized Delicate/preview database; never silently deduplicate or issue an immediate commit.

Read-only diagnostic:

```sql
SELECT "userId", COUNT(*) AS "duplicateCount"
FROM "PasswordResetToken"
GROUP BY "userId"
HAVING COUNT(*) > 1
ORDER BY "duplicateCount" DESC, "userId";
```

For every affected user, wait until the maximum per-row effective expiry has passed: `MAX(LEAST("createdAt" + INTERVAL '30 minutes', "expiresAt"))`. Then, in one transaction, lock and delete only used or effectively expired candidates:

```sql
BEGIN;
WITH duplicate_users AS (
  SELECT "userId"
  FROM "PasswordResetToken"
  GROUP BY "userId"
  HAVING COUNT(*) > 1
)
SELECT t."id", t."userId", t."createdAt", t."expiresAt", t."usedAt"
FROM "PasswordResetToken" AS t
JOIN duplicate_users AS d ON d."userId" = t."userId"
WHERE t."usedAt" IS NOT NULL
   OR LEAST(t."expiresAt", t."createdAt" + INTERVAL '30 minutes') <= CURRENT_TIMESTAMP
FOR UPDATE;

WITH duplicate_users AS (
  SELECT "userId"
  FROM "PasswordResetToken"
  GROUP BY "userId"
  HAVING COUNT(*) > 1
)
DELETE FROM "PasswordResetToken" AS t
USING duplicate_users AS d
WHERE t."userId" = d."userId"
  AND (t."usedAt" IS NOT NULL OR LEAST(t."expiresAt", t."createdAt" + INTERVAL '30 minutes') <= CURRENT_TIMESTAMP)
RETURNING t."id", t."userId", t."tokenFormat", t."createdAt", t."expiresAt", t."usedAt";
```

Stop with the transaction open for named-owner review. Only after explicit approval run this separate command in the same session:

```sql
COMMIT;
```

If approval is denied or review is incomplete, run this separate command instead:

```sql
ROLLBACK;
```

After a committed cleanup, re-run the exact `GROUP BY`/`HAVING` diagnostic and require zero rows; only then apply the migration. Never apply the unique index with duplicates remaining.

### Human-gated digest rollback after issuance

Pause password-reset issuance. In one transaction, lock all active digest rows and mark only those rows used:

```sql
BEGIN;
SELECT "id", "userId", "createdAt", "expiresAt", "usedAt"
FROM "PasswordResetToken"
WHERE "tokenFormat" = 'sha256' AND "usedAt" IS NULL
FOR UPDATE;

UPDATE "PasswordResetToken"
SET "usedAt" = CURRENT_TIMESTAMP
WHERE "tokenFormat" = 'sha256' AND "usedAt" IS NULL
RETURNING "id", "userId", "tokenFormat", "createdAt", "expiresAt", "usedAt";
```

Stop with the transaction open for named-owner review. Only after explicit approval run `COMMIT;` as a separate command; otherwise run `ROLLBACK;` as the separate command. After a committed invalidation, confirm zero active SHA-256 rows:

```sql
SELECT COUNT(*) AS "activeSha256Count"
FROM "PasswordResetToken"
WHERE "tokenFormat" = 'sha256' AND "usedAt" IS NULL;
```

Only after that zero-row confirmation perform the application rollback; users must request a fresh reset. Never interpret a digest as a raw token or weaken the legacy fallback.
